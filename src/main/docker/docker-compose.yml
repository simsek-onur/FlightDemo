services:
  postgres:
    image: postgres:16
    container_name: recon-postgres
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: recon
    ports:
      - "5467:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: